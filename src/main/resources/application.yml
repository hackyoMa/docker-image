server:
  shutdown: GRACEFUL
  port: 8080
spring:
  application:
    name: spring-boot-admin
  thymeleaf:
    check-template-location: false
  security:
    user:
      name: admin
      password: admin
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
